# 内容生成规范

- 模块 `README.md` 只负责目录、学习顺序、包含内容和完成状态。
- 每个基础知识点独立一个目录，使用 `kp001-standard-declaration` 格式命名。
- 知识点目录必须包含 `README.md`；文档统一提供学习目标、理论讲解、动手编码、完整源码讲解、运行案例、效果验证和课后练习。
- 动手编码必须从最小代码开始，分步骤构建到最终案例，并说明每一步“写什么、为什么、看到什么”。
- README 中最终构建出的代码必须与实际案例源码一致。
- 尚未学习的 JS/CSS 等仅作为辅助时，必须明确标注为“实验辅助代码”。
- 缺少动手编码过程的知识点不得标记为“已完成”。
- 理论讲解按照模块索引中的“包含内容”逐项展开。
- 综合项目统一放在模块的 `projects/` 目录下，使用 `c01-city-news` 格式命名。
- 综合项目编号使用 `C01`、`C02`，不占用 `KP` 编号。
- 源码文件结构由知识点或项目需要决定，不限制文件数量。
- 目录统一使用小写英文和连字符命名。
