# 内容生成规范

- 模块 `README.md` 只负责目录、学习顺序、包含内容和完成状态。
- 每个基础知识点独立一个目录，使用 `kp001-standard-declaration` 格式命名。
- 知识点目录必须包含 `README.md`。
- 知识点文档必须提供目录，并按照“学习目标 → 理论讲解 → 动手编码：从 0 到 1 → 完整源码讲解 → 运行案例 → 效果验证”的顺序组织正文。
- “动手编码：从 0 到 1”必须从空文件或最小可运行文件开始，逐步完成案例；每一步都要说明本步目标、需要编写的代码、为什么这样写，以及运行后应观察到什么。
- 文档必须明确区分“本节核心代码”和“实验辅助代码”，避免学习者把观察、展示或调试代码误认为知识点本身。
- 文档中的完整源码、文件路径、运行方式和预期结果必须与知识点目录中的实际源码保持一致。
- 理论讲解按照模块索引中的“包含内容”逐项展开。
- 综合项目统一放在模块的 `projects/` 目录下，使用 `c01-city-news` 格式命名。
- 综合项目编号使用 `C01`、`C02`，不占用 `KP` 编号。
- 源码文件结构由知识点或项目需要决定，不限制文件数量。
- 目录统一使用小写英文和连字符命名。
