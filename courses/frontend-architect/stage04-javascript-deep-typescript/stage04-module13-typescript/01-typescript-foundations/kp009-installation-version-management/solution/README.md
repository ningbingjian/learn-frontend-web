# TS-KP009 参考答案

关键结论：

1. 项目 TypeScript 版本由项目依赖声明，并由安装结果/lockfile 进一步固定实际依赖树。
2. `npx tsc` 可以使用项目本地工具链，减少依赖开发机器全局环境的差异。
3. 全局 `tsc` 是否存在、是什么版本，都不应该成为项目能否构建的唯一前提。
4. 升级 TypeScript 后至少应重新安装依赖、确认版本、执行类型检查和构建，并阅读目标版本迁移说明。

练习源码本身只是烟雾测试，重点是能够解释“项目版本”和“机器全局版本”的区别。
