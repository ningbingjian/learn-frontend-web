# TS-KP010 参考答案

- `-p` / `--project` 让 `tsc` 按指定 `tsconfig.json` 工作。
- `--showConfig` 展示继承、默认值和当前项目配置合并后的结果。
- 直接提供源文件参数时，本次调用不会自动采用附近项目的 `tsconfig.json`。
- 因此直接文件模式下如果希望严格检查，需要显式传入 `--strict` 等选项。
- `--noEmit` 只是禁止输出文件，不代表跳过解析或类型检查。
