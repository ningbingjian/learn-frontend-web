import { createElement } from 'react';
// TODO 1：从 react-dom/client 导入 createRoot

// TODO 2：创建一个 h1 React Element，内容为 Renderer boundary
const element = null;

// TODO 3：获取 #root DOM 容器
const container = null;

// TODO 4：使用 createRoot 创建 React DOM Root
const root = null;

// TODO 5：调用 root.render(element)

console.log({ element, container, root });
